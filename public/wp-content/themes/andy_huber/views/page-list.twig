{% extends 'base.twig' %}

{% block content %}
        <main>
    {% for post in posts %}
        <section>
            <h2> {{ post.title }} </h2>
            {% if post.thumbnail != null %}
                <img src="{{ post.thumbnail.src }}">
            {% endif %}
            {{ post.content }}
            <a class="white-button darkblue-button"href="{{ post.link }}">Read more</a>
        </section>
    {% endfor %}
        </main>
{% endblock %}