{% extends 'base.twig' %}

{% block content %}
        <main>
        <section>
            <h2> {{ post.title }} </h2>
            {% if post.thumbnail != null %}
                <img src="{{ post.thumbnail.src }}">
            {% endif %}
            {{ post.content }}
        </section>
        </main>
{% endblock %}